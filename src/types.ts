// types.ts or similar file

export interface SidebarContent {
    labResults: string;
    healthStatistics: string;
    summary: string;
    healthNeighbors: string;
    // Add logo if needed:
    // logo: string;
  }